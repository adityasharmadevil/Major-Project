# Server Configuration
server.port=8080
spring.application.name=PolicyShieldMonitor

# MongoDB Configuration
# Note: If DNS resolution fails, try using the standard connection string format instead
spring.data.mongodb.uri=mongodb+srv://Vercel-Admin-PolicyShieldMonitor:8HFJ7ppWkpbdZYCS@policyshieldmonitor.td4hcjt.mongodb.net/?retryWrites=true&w=majority
spring.data.mongodb.database=policyShieldMonitor
spring.data.mongodb.auto-index-creation=true
# Connection settings
spring.data.mongodb.timeout=10000

# Spring Configuration
spring.main.allow-bean-definition-overriding=true

# Spring Security Default User (for development)
spring.security.user.name=adi
spring.security.user.password=adi

# JWT Configuration
jwt.secret=your-secret-key-change-this-in-production-use-a-strong-random-string
jwt.expiration=86400000

# CORS Configuration
cors.allowed-origins=http://localhost:3000

# Logging
logging.level.com.dids=DEBUG
logging.level.org.springframework.security=DEBUG

