# Development Profile Configuration
# Use this profile for local development: --spring.profiles.active=dev

# Server Configuration
server.port=8080
spring.application.name=PolicyShieldMonitor

# MongoDB Configuration (Development)
# Use environment variables for sensitive data
spring.data.mongodb.uri=${MONGODB_URI:mongodb+srv://Vercel-Admin-PolicyShieldMonitor:8HFJ7ppWkpbdZYCS@policyshieldmonitor.td4hcjt.mongodb.net/?retryWrites=true&w=majority}
spring.data.mongodb.database=${MONGODB_DATABASE:policyShieldMonitor}
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.timeout=10000

# Spring Configuration
spring.main.allow-bean-definition-overriding=true

# JWT Configuration (Development - Use strong secret in production)
jwt.secret=${JWT_SECRET:dev-secret-key-change-this-in-production-use-a-strong-random-string-min-256-bits}
jwt.expiration=${JWT_EXPIRATION:86400000}

# CORS Configuration (Development)
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:3001}

# Logging (Development - More verbose)
logging.level.com.dids=DEBUG
logging.level.org.springframework.security=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.mongodb.driver=INFO

# Actuator (Development - More endpoints exposed)
management.endpoints.web.exposure.include=health,info,metrics,env
management.endpoint.health.show-details=always

# DevTools
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true

